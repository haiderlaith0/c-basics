#include <stdio.h>
#include <math.h>

int main() {
    
    double number = 64.0;
    double result = pow(number, 64.0);
    printf("The square root of %.2f is %.2f\n", number, result);


    return 0;
}